{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Config",
  "type": "object",
  "properties": {
    "components": {
      "default": {},
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "$ref": "#/definitions/Component"
      }
    }
  },
  "definitions": {
    "BufferConfig": {
      "description": "Per-sink delivery buffer (`docs/adr/0020-buffered-sink-delivery.md`). Meaningful only on a sink; graph validation (`crates/logit-pipeline/src/graph.rs`) rejects a non-default value on any other kind. Every field defaults, so an omitted `buffer:` block is exactly today's behavior.",
      "type": "object",
      "properties": {
        "delivery": {
          "description": "`None` -- the default -- means \"derive from the sink's own `duplicate_safe()` fact\" (`docs/adr/0020-buffered-sink-delivery.md`'s three-layer posture design). `Some(_)` overrides that default for this component specifically.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/DeliveryPosture"
            },
            {
              "type": "null"
            }
          ]
        },
        "max_batches": {
          "default": 1024,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "max_bytes": {
          "description": "Byte bound on the buffer's estimated heap footprint (`EventBatch::estimated_heap_bytes`), checked alongside `max_batches` -- whichever trips first. Human-readable in YAML (`64MiB`, `134217728`) via the [`human_bytes`] codec below, not a bare byte count.",
          "default": 67108864,
          "type": "string"
        },
        "overflow": {
          "default": "block",
          "allOf": [
            {
              "$ref": "#/definitions/OverflowPolicy"
            }
          ]
        },
        "retry_budget": {
          "default": "60s",
          "type": "string"
        },
        "retry_max_delay": {
          "default": "10s",
          "type": "string"
        },
        "shutdown_grace": {
          "default": "5s",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "Component": {
      "description": "One node in the pipeline's component graph. `sources` names the other components this one reads events from -- empty for a listener, required for everything else (enforced at validation time, not in this schema: which arity is legal depends on `kind`, not something a blanket `minItems` on this shared field can express). See `docs/design/pipeline-graph.md` for the full arity table and validation rules.",
      "type": "object",
      "oneOf": [
        {
          "description": "statsd / DogStatsD-style tagged metrics over UDP.",
          "type": "object",
          "required": [
            "bind",
            "type"
          ],
          "properties": {
            "bind": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "statsd_in"
              ]
            }
          }
        },
        {
          "description": "RFC 3164 / RFC 5424 syslog over UDP or TCP.",
          "type": "object",
          "required": [
            "bind",
            "type"
          ],
          "properties": {
            "bind": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "syslog_in"
              ]
            }
          }
        },
        {
          "description": "OpenTelemetry Protocol (logs, metrics, and/or traces).",
          "type": "object",
          "required": [
            "bind",
            "type"
          ],
          "properties": {
            "bind": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "otlp_in"
              ]
            }
          }
        },
        {
          "description": "Tail one or more files as a log source, rotation- and checkpoint-aware.",
          "type": "object",
          "required": [
            "paths",
            "type"
          ],
          "properties": {
            "checkpoint_path": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "paths": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "file_tail"
              ]
            }
          }
        },
        {
          "description": "The native logit-to-logit protocol (`docs/design/wire-protocol.md`).",
          "type": "object",
          "required": [
            "bind",
            "type"
          ],
          "properties": {
            "bind": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "logit_in"
              ]
            }
          }
        },
        {
          "description": "`logit` talking about itself: drains every component's buffered self-telemetry points on `interval` and emits them as ordinary events into the graph, same as any other listener. Named for the source, not the signal it emits today -- free to grow logs and spans later without a rename. See `docs/design/internal-telemetry.md` and `docs/adr/0018-internal-telemetry-as-pipeline-events.md`.",
          "type": "object",
          "required": [
            "interval",
            "type"
          ],
          "properties": {
            "interval": {
              "description": "Both the drain cadence for every component's buffered points and the sampling tick for this component's own process-level gauges (interner size, uptime). Should divide evenly into any downstream `aggregate` interval, or the two windows beat against each other.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "internal"
              ]
            }
          }
        },
        {
          "description": "Inline Lua source (a YAML block scalar in practice). See `docs/design/lua-api.md`.",
          "type": "object",
          "required": [
            "script",
            "type"
          ],
          "properties": {
            "interval": {
              "description": "Runs this component's `flush()`, if the script defines one, on this interval (`docs/design/lua-api.md`'s flush contract). Omitted -- the common case -- means the component never ticks, same as a script with no `flush()` at all.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "script": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "lua"
              ]
            }
          }
        },
        {
          "description": "A `.lua` file path, relative to the config file.",
          "type": "object",
          "required": [
            "lua_file",
            "type"
          ],
          "properties": {
            "interval": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "lua_file": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "lua_file"
              ]
            }
          }
        },
        {
          "description": "The stateful aggregator (counters/gauges/sets/distributions). Runs `flush()` on `interval`; see `docs/adr/0008-aggregation-window-semantics.md`.",
          "type": "object",
          "required": [
            "interval",
            "type"
          ],
          "properties": {
            "interval": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "aggregate"
              ]
            }
          }
        },
        {
          "description": "Parses a log record's message as JSON, merging the resulting key/values into the event's attributes. See `docs/adr/0010-json-parsing-into-attributes.md`.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "skip_to_brace": {
              "description": "Skip everything before the first `{` and parse from there -- for lines with a non-JSON prefix (`2026-08-29 INFO {\"a\":1}`). Off by default: the whole line is assumed to be the JSON data.",
              "default": false,
              "type": "boolean"
            },
            "type": {
              "type": "string",
              "enum": [
                "json"
              ]
            }
          }
        },
        {
          "description": "Turns attributes already on an event (typically merged there by `json`) into metrics on that same event. See `docs/adr/0014-kv-metrics-semantics.md` for the skip rules, the numeric coercion rules, and why there is deliberately no `tags:` field here -- tag selection is `Keep`'s job, since every metrics sink already reads `event.attributes`.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "counters": {
              "default": [],
              "type": "array",
              "items": {
                "$ref": "#/definitions/MetricSpec"
              }
            },
            "distributions": {
              "default": [],
              "type": "array",
              "items": {
                "$ref": "#/definitions/MetricSpec"
              }
            },
            "gauges": {
              "default": [],
              "type": "array",
              "items": {
                "$ref": "#/definitions/MetricSpec"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "kv_metrics"
              ]
            }
          }
        },
        {
          "description": "Retains only the named attributes, dropping the rest -- an allowlist, not just a denylist: a new field appearing in a log format later must not be able to silently become a new tag dimension on a metrics sink. Place this *before* `aggregate` in a pipeline -- `aggregate`'s `SeriesKey` includes the whole of `event.attributes`, so pruning first is what keeps series cardinality and per-window memory bounded. An empty `fields` list is legal and means \"drop every attribute.\"",
          "type": "object",
          "required": [
            "fields",
            "type"
          ],
          "properties": {
            "fields": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "keep"
              ]
            }
          }
        },
        {
          "description": "Drops the named attributes, keeping the rest.",
          "type": "object",
          "required": [
            "fields",
            "type"
          ],
          "properties": {
            "fields": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "enum": [
                "remove"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "logfmt"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "kv"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "pattern",
            "type"
          ],
          "properties": {
            "pattern": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "regex"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "csv"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "from",
            "to",
            "type"
          ],
          "properties": {
            "from": {
              "type": "string"
            },
            "to": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "rename"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "type",
            "where"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "filter"
              ]
            },
            "where": {
              "type": "string"
            }
          }
        },
        {
          "type": "object",
          "required": [
            "rate",
            "type"
          ],
          "properties": {
            "rate": {
              "type": "number",
              "format": "double"
            },
            "type": {
              "type": "string",
              "enum": [
                "sample"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "limit",
            "type",
            "window"
          ],
          "properties": {
            "limit": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "type": {
              "type": "string",
              "enum": [
                "throttle"
              ]
            },
            "window": {
              "type": "string"
            }
          }
        },
        {
          "type": "object",
          "required": [
            "key",
            "type"
          ],
          "properties": {
            "key": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "dedup"
              ]
            }
          }
        },
        {
          "description": "`rename`d explicitly: `rename_all = \"snake_case\"` alone would tag this `influx_db_out` (a word break at the embedded capital `Db`), not `influxdb_out` as published in `docs/design/pipeline-graph.md` and every example config.",
          "type": "object",
          "required": [
            "bucket",
            "org",
            "token",
            "type",
            "url"
          ],
          "properties": {
            "bucket": {
              "type": "string"
            },
            "org": {
              "type": "string"
            },
            "token": {
              "description": "A plain string field like any other -- give it `!env INFLUXDB_TOKEN` in config to pull it from the environment (`crates/logit-cli/src/config.rs`) rather than inlining it. No env-specific field of its own: `!env` works on any field on any component, so `url`/`org`/`bucket` (just as deployment-specific) can use it too.",
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "influxdb_out"
              ]
            },
            "url": {
              "type": "string"
            }
          }
        },
        {
          "type": "object",
          "required": [
            "endpoint",
            "type"
          ],
          "properties": {
            "endpoint": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "otlp_out"
              ]
            }
          }
        },
        {
          "description": "The native logit-to-logit protocol (`docs/design/wire-protocol.md`).",
          "type": "object",
          "required": [
            "endpoint",
            "type"
          ],
          "properties": {
            "endpoint": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "enum": [
                "logit_out"
              ]
            }
          }
        },
        {
          "description": "A general-purpose, human-facing debug sink: dumps every event's details as a readable text block to stdout (default), stderr, or a file -- the dev loop for seeing a whole pipeline's output without standing up a real backend like InfluxDB.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "target": {
              "default": "stdout",
              "allOf": [
                {
                  "$ref": "#/definitions/StdioTarget"
                }
              ]
            },
            "type": {
              "type": "string",
              "enum": [
                "stdio_out"
              ]
            }
          }
        }
      ],
      "properties": {
        "buffer": {
          "description": "Per-sink delivery buffer (`docs/adr/0020-buffered-sink-delivery.md`). Meaningful only on a sink -- graph validation (`crates/logit-pipeline/src/graph.rs`) rejects a non-default value on any other kind. A sibling field of `kind`, not nested inside every sink `ComponentKind` variant, so a future fifth sink kind costs nothing extra here.",
          "default": {
            "delivery": null,
            "max_batches": 1024,
            "max_bytes": 67108864,
            "overflow": "block",
            "retry_budget": "60s",
            "retry_max_delay": "10s",
            "shutdown_grace": "5s"
          },
          "allOf": [
            {
              "$ref": "#/definitions/BufferConfig"
            }
          ]
        },
        "sources": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "DeliveryPosture": {
      "description": "Whether re-delivering an already-delivered batch is safe for a sink's destination -- see `BufferConfig::delivery`. `logit-config`'s own copy, for the same crate-layout reason as [`OverflowPolicy`]; converted to `logit_pipeline::DeliveryPosture` in `crates/logit-cli/src/pipeline.rs`.",
      "type": "string",
      "enum": [
        "at_least_once",
        "at_most_once"
      ]
    },
    "MetricSpec": {
      "description": "One `kv_metrics` entry: a metric `name`, an optional source `field`, and an optional `unit`. See `ComponentKind::KvMetrics` and `docs/adr/0014-kv-metrics-semantics.md`.",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "field": {
          "description": "The attribute to read this metric's value from. Omitted means \"+1 per event\" for a counter or \"set to 1\" for a gauge; a distribution entry with no `field` is rejected at graph-validation time (a distribution of nothing is meaningless). Names an attribute literally -- `field: http.status` means the attribute literally named `http.status`, never a `status` key nested under `http` in a `Value::Map`; nested fields are not addressable.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The metric's measurement name. An empty name is rejected at graph-validation time -- `influxdb_out` requires a non-empty measurement to encode a metric line.",
          "type": "string"
        },
        "unit": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "OverflowPolicy": {
      "description": "What a sink's `SinkQueue` does once both its bounds (`max_batches`/`max_bytes`) are full. `logit-config`'s own copy -- `logit-config` must not depend on `logit-pipeline` (`docs/design/pipeline-graph.md`'s crate layout), so `crates/logit-cli/src/pipeline.rs` converts this to `logit_pipeline::OverflowPolicy` when building a `NodeSpec::Output`.",
      "type": "string",
      "enum": [
        "block",
        "drop_oldest",
        "drop_newest"
      ]
    },
    "StdioTarget": {
      "type": "string"
    }
  }
}
