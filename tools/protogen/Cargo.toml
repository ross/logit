[package]
name = "protogen"
version = "0.1.0"
edition = "2021"
publish = false

# Its own empty workspace, outside the root one, so `prost-build` and `protoc` never enter a
# build-time or CI dependency graph (docs/adr/committed-pregenerated-otlp-protobuf.md).
[workspace]

[dependencies]
prost-build = "0.14"
