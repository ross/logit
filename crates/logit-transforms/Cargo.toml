[package]
name = "logit-transforms"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Built-in native transform stages (no Lua VM involved) -- aggregate first."

[dependencies]
logit-core.workspace = true
logit-pipeline.workspace = true
thiserror.workspace = true
bytes.workspace = true
serde.workspace = true
serde_json.workspace = true
smallvec.workspace = true
regex.workspace = true

[dev-dependencies]
# Only `chained_pipeline_test` needs these -- driving a real graph through
# `logit_pipeline::run`/`Fanout` (see `provenance::tests`'s end-to-end test), not just calling
# `Transform::process` directly the way every other test in this crate does.
anyhow.workspace = true
async-trait.workspace = true
logit-config.workspace = true
tokio = { workspace = true, features = ["rt", "macros"] }
